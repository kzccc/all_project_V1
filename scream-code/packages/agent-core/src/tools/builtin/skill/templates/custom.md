# Skill type: custom

The user has chosen a custom focus. Honor the user's explicit hint while still grounding the skill in the current conversation.

If the hint specifies a format (e.g. "API design checklist"), follow that format. Otherwise, produce a clear, actionable skill that includes:
1. What the skill does.
2. When to use it.
3. The core instructions or steps.
4. A concrete example grounded in the conversation.

Stay concise and reusable across projects.
