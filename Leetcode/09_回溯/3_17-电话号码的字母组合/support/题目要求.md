# 17. 电话号码的字母组合

在根目录 `answer.go` 中实现：

```go
func letterCombinations(digits string) []string
```

本地输入：一行数字字符串，例如：

```text
23
```

本地输出：

- 第一行输出组合数量
- 后续每行一个字符串

在 `support/` 目录下执行：

```bash
go test
```
