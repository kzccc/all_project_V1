# 78. 子集

你需要在根目录 `answer.go` 中实现：

```go
func subsets(nums []int) [][]int
```

本地输入：

```text
n
nums[0] nums[1] ... nums[n-1]
```

本地输出：

- 第一行输出子集总数
- 后续每行格式为：`长度 元素1 元素2 ...`
- 空集输出为单独一行 `0`

在 `support/` 目录下运行：

```bash
go test
```
