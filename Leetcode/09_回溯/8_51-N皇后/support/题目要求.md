# 51. N 皇后

在根目录 `answer.go` 中实现：

```go
func solveNQueens(n int) [][]string
```

本地输入：

```text
n
```

本地输出：

- 第一行输出方案数量
- 后续每行输出一个解，格式是每一行棋盘字符串用 `|` 连接

例如 `n=4` 的一个解：

```text
.Q..|...Q|Q...|..Q.
```

在 `support/` 目录执行：

```bash
go test
```
