# 46. 全排列

你需要在根目录 `answer.go` 中实现：

```go
func permute(nums []int) [][]int
```

本地输入格式：

```text
n
nums[0] nums[1] ... nums[n-1]
```

本地输出格式：

- 第一行输出答案数量
- 后续每行格式为：`长度 元素1 元素2 ...`

只修改 `../answer.go`。

在 `support/` 目录下运行：

```bash
go test
```
