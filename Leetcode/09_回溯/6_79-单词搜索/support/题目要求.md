# 79. 单词搜索

在根目录 `answer.go` 中实现：

```go
func exist(board [][]byte, word string) bool
```

本地输入：

```text
rows cols
第1行字符串
第2行字符串
...
word
```

本地输出：

- `true` 或 `false`

在 `support/` 目录执行：

```bash
go test
```
