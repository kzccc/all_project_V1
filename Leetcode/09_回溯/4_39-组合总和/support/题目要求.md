# 39. 组合总和

在根目录 `answer.go` 中实现：

```go
func combinationSum(candidates []int, target int) [][]int
```

本地输入：

```text
n target
candidates[0] candidates[1] ... candidates[n-1]
```

本地输出：

- 第一行输出组合数量
- 后续每行格式为：`长度 元素1 元素2 ...`

在 `support/` 目录下执行：

```bash
go test
```
