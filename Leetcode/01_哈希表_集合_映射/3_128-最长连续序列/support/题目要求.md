# 128. 最长连续序列

给定一个未排序整数数组，求最长连续数字序列的长度。

你需要在根目录 `answer.go` 中实现：

```go
func longestConsecutive(nums []int) int
```

本地输入格式：

```text
n
nums[0] nums[1] ... nums[n-1]
```

本地输出格式：

```text
最长连续序列长度
```

只修改 `../answer.go`。

在 `support/` 目录下运行：

```bash
go test
```
