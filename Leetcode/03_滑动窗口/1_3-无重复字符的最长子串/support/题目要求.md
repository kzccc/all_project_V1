# 3. 无重复字符的最长子串

给定一个字符串，求不含重复字符的最长子串长度。

你需要在根目录 `answer.go` 中实现：

```go
func lengthOfLongestSubstring(s string) int
```

本地输入格式：

```text
s
```

本地输出格式：

```text
最长长度
```

说明：

- 本地训练输入使用单个不含空格的字符串。

只修改 `../answer.go`。

在 `support/` 目录下运行：

```bash
go test
```
