# 11. 盛最多水的容器

给定一个数组 `height`，其中每个值表示竖线高度，求两条线和 x 轴围成的最大面积。

你需要在根目录 `answer.go` 中实现：

```go
func maxArea(height []int) int
```

本地输入格式：

```text
n
height[0] height[1] ... height[n-1]
```

本地输出格式：

```text
最大面积
```

只修改 `../answer.go`。

在 `support/` 目录下运行：

```bash
go test
```
