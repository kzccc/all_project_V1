# echochat 路由污染治理：心跳续约、Redis TTL 与客户端重连设计

## 1. 文档目标

本文档只聚焦 `echochat` 在线路由污染治理，不展开消息主链路、Kafka 批处理、会话摘要、未读数或多端产品能力。

本文档要解决三件事：

1. 设计一套可落地的服务端心跳续约机制，保证连接失效后系统能够快速收敛本地连接表和 Redis 路由表。
2. 设计一套路由 TTL 机制，保证在进程 crash、清理失败或连接异常滞后时，Redis 脏路由仍可自动过期。
3. 设计一套客户端自动重连机制，保证连接中断后能够自动恢复在线能力，并补齐断线期间遗漏的数据。

## 2. 问题定义

当前因为某些原因，客户端与服务端的连接出现了异常，但是服务端没能及时发现用户真实连接状态已经失效或变更了，系统里的在线路由记录还指向旧实例或旧连接，导致后续消息被错误寻址，这就是路由污染。

本质上是当前在线路由系统缺少 TTL 和租约机制，导致连接失效后状态不能及时收敛，最终形成路由污染。

典型场景有三类：

1. 实例异常退出：进程 crash、容器被 kill 或 OOM 时，路由已写入 Redis，但进程来不及执行注销清理，导致实例已不存在而 Redis 仍保留旧路由。
2. 连接异常断开：断网、网络切换或网络波动时，服务端未能及时感知并执行 `RemoveClient`，导致用户实际已离线但 Redis 仍标记为在线。
3. 清理链路不完整：本地连接删除成功但 Redis 删除失败，由于本地和 Redis 操作非原子，导致本机已确认用户下线而跨实例仍按旧路由投递。

## 3. 设计原则

1. 实时投递必须以连接层健康为前提，在线路由只能作为寻址依据，不能代替真实连接健康判断。
2. 本地连接表与 Redis 路由表职责不同，不要求两者使用完全相同的维护机制。
3. 本地连接表负责维护当前实例内真实存在的 websocket 连接对象。
4. Redis 路由表负责把“当前可投递目标”以租约方式同步给其他实例。
5. 心跳周期、最长静默窗口与 Redis TTL 的关系必须满足：`心跳周期 < 最长静默窗口 < Redis TTL`。
6. Redis TTL 是兜底自动失效机制，不替代连接层主动判死。

## 4. 当前系统边界与改造目标

### 4.1 当前系统边界

当前系统存在两张“在线表”：

1. 本地内存连接表
   - `channel` 模式：`Server.Clients map[string]*Client`
   - `kafka` 模式：`KafkaServer.Clients map[string]*Client`
2. Redis 跨实例在线路由表
   - key：`echochat:kafka:ws_route:{clientID}`
   - value：当前仅为 `instance_id`

当前系统的真实语义不是“完整在线状态系统”，而是“在线投递路由系统”。

### 4.2 本次改造目标

本次设计只解决以下问题：

1. 如何判断本地连接是否已经失效。
2. 如何在连接失效后及时删除本地连接表与跨实例路由。
3. 如何在实例 crash 或清理失败时依赖 TTL 自动收敛 Redis 脏路由。
4. 如何让客户端在断线后自动重连，并在重连成功后恢复在线能力。

本次不解决以下问题：

1. 多端在线集合模型。
2. 设备粒度踢下线。
3. 产品层“最近在线时间”展示。
4. 在线状态查询 API。

## 5. 核心对象与职责划分

### 5.1 本地连接表

本地连接表保存的是当前实例内真实 websocket 连接对象。

职责：

1. 提供当前实例内的直接实时投递能力。
2. 作为连接层心跳探测和连接健康判断的承载对象。
3. 在连接失效时第一时间停止本机继续向该连接写消息。

### 5.2 Redis 路由表

Redis 路由表保存的是跨实例可见的在线路由记录。

职责：

1. 当消息落在非目标实例时，为消息提供跨实例寻址能力。
2. 通过 TTL 和续约机制对外声明“这条连接当前仍然有效”。
3. 在本地清理失效或实例异常退出时，通过主动删除或 TTL 到期完成路由收敛。

### 5.3 连接层与在线路由层

连接层回答的是：“这条 websocket 连接现在还活着吗？”

在线路由层回答的是：“系统当前还要不要把这条连接作为可投递目标对外发布？”

二者关系是：

1. 连接层提供真实连接事实。
2. 在线路由层把这份真实事实发布为跨实例可见的租约记录。

## 6. 服务端心跳续约机制设计

## 6.1 目标

服务端通过 websocket 协议层 `ping/pong` 维护本地连接健康，确保连接层能够在合理时间内发现失效连接，并触发本地连接表与 Redis 路由表的同步收敛。

## 6.2 机制说明

服务端为每条连接维护两个核心参数：

1. 心跳探测周期
   - 服务端每隔固定时间向客户端发送一次 `ping`。
2. 最长静默窗口
   - 只要在这个窗口内 websocket 读取到任何有效消息，都会刷新该窗口。

有效消息包括：

1. 客户端发送的业务消息。
2. 客户端对服务端 `ping` 的响应 `pong`。
3. 客户端主动发送的 heartbeat 消息（如果后续前端协议补充此能力）。

如果在最长静默窗口内始终没有读取到任何有效消息，一旦超过窗口上限，连接层就会判定这条连接已经失效。

## 6.3 参数设置

### 6.3.1 心跳探测周期

建议值：`15s`

设置依据：

1. 需要在较短时间内发现坏连接，但不能把心跳打得过密。
2. 当前 `echochat` 为 IM 长连接场景，连接数上来后心跳过密会拉高调度和网络开销。
3. `15s` 既能较快探测断线，也适合作为 Redis 路由续约周期。

过大影响：

1. 死连接发现变慢。
2. 本地脏连接停留更久。
3. 实时投递对坏连接抱希望的时间更长。

过小影响：

1. 心跳包过于密集，服务端调度和网络开销明显增加。
2. 弱网环境下误判概率上升。

### 6.3.2 最长静默窗口

建议值：`40s`

设置依据：

1. 最长静默窗口与心跳周期强绑定。
2. 若心跳周期记为 `H`，额外缓冲记为 `G`，则静默窗口至少应为 `H + G`。
3. 本方案允许一次短时抖动并保留少量调度缓冲，因此采用 `2H + G` 的设计。
4. 当 `H = 15s`，取 `G = 10s`，则得到 `40s`。

过大影响：

1. 连接判死过慢。
2. 本地连接污染停留时间更久。
3. 整体路由收敛速度变慢。

过小影响：

1. 网络抖动、短时卡顿或调度延迟可能导致误杀健康连接。
2. 用户侧更容易出现频繁断线和反复重连。

## 6.4 连接失效后的系统行为

当连接层判定某条 websocket 连接已经失效时，系统必须立刻执行以下动作：

1. 主动关闭该 websocket 连接。
2. 从本地连接表中删除该连接，使系统不再把它视为当前实例内的可投递目标，也不会再基于它做实时推送。
3. 同步收敛 Redis 在线路由或在线状态记录，删除或让其过期，避免其他实例继续把消息错误路由到这条失效连接或旧实例。
4. 该连接失效后，系统后续再向该用户推送消息时，将不再把这条连接当作可用实时投递目标；与失效前可以直接通过该连接实时推送相比，失效后系统只能尝试该用户的其他健康连接，若不存在其他可用连接，则消息不再走实时直推，而转入离线或后续同步路径。

## 6.5 哪些事件刷新 Redis TTL

Redis 路由 TTL 不应因为服务端本地定时器到点或单纯“发出了 ping”就刷新，只有能证明客户端真实活着的事件才允许续约。

允许刷新 Redis TTL 的事件：

1. 连接建立成功。
2. 收到客户端对服务端 `ping` 的有效 `pong` 响应。
3. 收到客户端发来的有效业务消息。
4. 收到客户端主动 heartbeat 消息（如果后续前端补充此协议）。

不允许刷新 Redis TTL 的事件：

1. 服务端本地心跳 timer 到点。
2. 服务端刚刚发出 `ping`，但尚未收到任何客户端反馈。
3. 单纯进入下一轮读循环，但未读到有效消息。

## 7. Redis 路由 TTL 机制设计

## 7.1 目标

在进程 crash、清理失败或连接异常滞后时，Redis 路由仍然能通过 TTL 自动失效机制完成兜底收敛，避免 stale route 长时间残留。

## 7.2 路由记录结构

当前 Redis 路由表 value 为纯字符串 `instance_id`，本次改造后应升级为结构化记录，至少包含以下字段：

```json
{
  "instance_id": "chat-18082",
  "connection_id": "WS123456",
  "active_at_unix": 1760600000,
  "connected_at_unix": 1760599960
}
```

字段要求：

1. `instance_id`
   - 跨实例消息寻址所必需。
2. `connection_id`
   - 防止旧连接清理误删新连接路由。
3. `active_at_unix`
   - 记录最近一次有效客户端活动时间。
4. `connected_at_unix`
   - 记录本次连接建立时间，便于排查和观测。

## 7.3 路由 TTL 参数

建议值：`60s`

设置依据：

1. Redis TTL 必须大于最长静默窗口，不能出现连接层还认为健康但 Redis 先过期的情况。
2. Redis TTL 不能太大，否则 crash 后脏路由停留过久，兜底收敛意义变弱。
3. 本文采用“Redis TTL 约为最长静默窗口的 1.5 倍”的保守口径。
4. 当最长静默窗口为 `40s` 时，Redis TTL 设为 `60s`。

过大影响：

1. 进程 crash 后 stale route 残留更久。
2. 其他实例继续错误寻址的窗口更长。

过小影响：

1. 健康连接仍存活时，Redis 路由可能提前过期。
2. 其他实例会误以为用户离线，产生“假离线”。

## 7.4 Redis 续约周期

Redis 续约周期与服务端心跳探测周期保持一致，建议值：`15s`。

设计原因：

1. 连接层的健康探测与 Redis 路由续约保持一致，便于行为统一。
2. 只有服务端获得了来自客户端的有效活动反馈，才执行一次续约。
3. 续约不是在旧 TTL 上累加，而是每次把过期时间重置回固定值 `60s`。

## 7.5 进程 crash 时的兜底行为

对于进程 crash、容器被 kill、节点宕机等无法执行正常清理链路的场景，Redis 在线路由不依赖主动删除，而依赖 TTL 自动失效。

流程如下：

1. 实例存活期间，连接层在有效活动到来时持续续约 Redis 路由。
2. 一旦实例异常退出，续约会立即停止。
3. 由于没有新的续约，这条 Redis 路由将在 `60s` 后自然过期。
4. 其他实例后续再查该用户路由时，将查不到旧实例，从而完成脏路由兜底收敛。

## 7.6 删除策略

服务端在主动清理 Redis 路由时，应采用“按 `connection_id` 精确删除”的策略，而不是仅按 `user_id` 或 `instance_id` 粗粒度删除。

原因：

1. 用户快速重连时，旧连接的清理可能晚于新连接注册。
2. 如果删除只按 `instance_id` 匹配，同一实例内旧连接有机会误删新连接的在线路由。

因此本次改造要求：

1. Redis 删除逻辑必须校验 `connection_id`。
2. 只有当前 Redis 记录中的 `connection_id` 与待清理连接一致时，才允许删除。

## 8. 客户端重连设计

## 8.1 目标

当连接层断开后，客户端能够在登录态有效的前提下自动恢复在线能力，并在重连成功后补齐断线期间遗漏的数据。

## 8.2 断线感知

客户端不依赖已经失效的旧 websocket 再发 ping，而是通过本地状态机感知断线。

主要触发点保留三类：

1. 收到 websocket `close` 或 `error` 时，客户端会把它视为一次非预期断连并进入自动重连流程。
2. 当应用从后台回到前台时，客户端会重新检查连接健康状态，并在连接不可用时立即重连。
3. 当客户端发现当前登录态仍有效但本地已经没有可用连接时，会主动补发起重连。

## 8.3 自动重连执行逻辑

客户端自动重连由本地 timer 或带退避的任务调度器驱动，到时间就尝试创建一条新的 websocket，失败后按下一轮时间继续重试。

每一轮重连通常执行以下四步：

1. 每一轮重连开始前，客户端都会先检查当前是否仍然允许重连，例如用户仍处于登录态、未被踢下线、token 未失效且业务上仍需要保持在线。
2. 如果允许重连，客户端会重新发起一次全新的 WebSocket 建连流程，包括重新建立 TCP 连接、重新执行 WebSocket 握手，并创建新的连接对象。
3. 新连接建立过程中，客户端还会重新携带 token 以及 userID、deviceID、platform 等身份信息完成鉴权与注册，使服务端重新建立本地连接表和在线路由记录。
4. 一旦连接成功，客户端会停止当前重连定时器、清空失败计数，并补拉断线期间未同步的消息与状态数据，从而恢复正常在线状态。

## 8.4 自动重连参数

### 8.4.1 首轮快速重试

建议序列：

1. 第 1 次：立即重连。
2. 第 2 次：`1s` 后。
3. 第 3 次：`2s` 后。
4. 第 4 次：`5s` 后。
5. 第 5 次：`10s` 后。

设计原因：

1. 连接刚断开时，短时间故障有较高概率快速恢复。
2. 前几轮更激进，目的是争取快速恢复用户实时能力。

### 8.4.2 退避重试

在快速重试阶段仍未成功后，进入退避重试阶段：

1. 后续每 `30s` 重试一次。
2. 自动重连最长持续时间设为 `15min`。

设计原因：

1. 退避重试可以避免无限高速空转，降低对服务端与网络的冲击。
2. `15min` 不是“明确不可重连条件”，而是自动重连保护上限。
3. 超过 `15min` 仍未成功时，客户端停止自动重连，等待用户手动触发或新的网络状态变化再重新进入重连流程。

### 8.4.3 停止重连条件

客户端在以下情况下停止自动重连：

1. 用户主动退出登录。
2. token 失效或鉴权失败。
3. 服务端明确返回踢下线、封禁或不允许继续在线的业务结果。
4. 自动重连持续超过 `15min`，进入保护暂停状态。

## 9. 代码执行计划

## 9.1 服务端后端改造

### A. websocket 连接层

涉及文件：

1. `internal/service/chat/client.go`
2. `internal/service/chat/kafka_server.go`
3. `internal/service/chat/server.go`

执行项：

1. 为 `Client` 增加 `lastActiveAt`、`heartbeatState`、`closeReason` 等必要字段。
2. 在 websocket 连接初始化阶段增加 `ping/pong` 处理逻辑。
3. 为每个连接启动服务端心跳探测 timer，按 `15s` 周期发送 `ping`。
4. 在收到业务消息或 `pong` 时刷新连接静默窗口。
5. 当超过 `40s` 静默窗口未读到有效消息时，主动关闭连接并进入统一清理流程。
6. 将清理逻辑统一收口，确保 `Read`、`Write`、心跳超时、主动登出最终都走同一套摘路由和删本地连接逻辑。

### B. Redis 路由层

涉及文件：

1. `internal/service/chat/kafka_instance_dispatch.go`
2. `internal/service/redis/redis_service.go`

执行项：

1. 新增结构化路由记录模型，替换当前纯字符串 `instance_id`。
2. 将 `registerClientRoute` 从永久 `SetKey` 改为带 TTL 的 `SetKeyEx`。
3. 新增“刷新路由 TTL 与 `active_at`”的续约方法。
4. 新增按 `connection_id` Compare-And-Delete 的 Redis 删除逻辑。
5. 远端路由查询逻辑升级为解析结构化记录，并保留路由不存在、路由结构非法、路由已过期等异常兜底分支。

### C. 服务停机与实例异常场景

涉及文件：

1. `internal/service/chat/kafka_server.go`
2. `internal/service/chat/server.go`

执行项：

1. 关机时尽量遍历当前实例连接并主动删除对应 Redis 路由。
2. 明确实例 crash 场景不依赖主动删除，而依赖 Redis TTL 收敛。
3. 记录连接关闭原因与 Redis 路由清理结果，便于观测和排障。

## 9.2 客户端改造

前端或客户端 SDK 需要补齐以下能力：

1. 明确 websocket `close` / `error` 回调进入自动重连状态机。
2. 应用前后台切换时增加连接健康检查。
3. 维护本地重连 timer 与失败计数。
4. 按“立即、1s、2s、5s、10s、30s 循环”执行重连退避。
5. 自动重连持续超过 `15min` 后进入暂停状态。
6. 重连成功后自动补拉断线期间消息与状态数据。

## 9.3 配置项设计

建议在配置文件中新增以下参数：

1. `ws_heartbeat_interval = 15s`
2. `ws_silence_timeout = 40s`
3. `ws_route_ttl = 60s`
4. `ws_reconnect_fast_retry = [0s,1s,2s,5s,10s]`
5. `ws_reconnect_backoff = 30s`
6. `ws_reconnect_max_window = 15m`

要求：

1. 所有参数必须配置化，不允许关键运行参数全部写死在代码常量中。
2. 默认值以上述建议值为准，后续可通过压测和灰度调整。

## 9.4 观测与测试计划

### 观测项

1. 当前在线连接数。
2. 心跳发送次数、心跳成功次数、心跳超时次数。
3. Redis 路由注册次数、续约次数、删除次数、TTL 过期次数。
4. stale route 命中次数。
5. 客户端重连成功次数、重连失败次数、进入 `15min` 暂停状态次数。

### 测试场景

1. 客户端正常在线收发消息。
2. 客户端断网后服务端在 `40s` 内判死并删本地连接。
3. 断网后 Redis 路由在服务端主动清理成功时立即消失。
4. 进程 crash 后 Redis 路由在 `60s` 内自动过期。
5. 用户快速重连时旧连接不会误删新连接路由。
6. 客户端自动重连成功后能补回断线期间消息。

## 10. 最终参数口径

本方案首版参数统一定为：

1. 服务端心跳探测周期：`15s`
2. 最长静默窗口：`40s`
3. Redis 路由 TTL：`60s`
4. 客户端快速重连序列：`0s / 1s / 2s / 5s / 10s`
5. 客户端退避重连周期：`30s`
6. 客户端自动重连最长持续时间：`15min`

## 11. 一句话总括

本方案将当前“登录时写一次、依赖正常清理删除”的在线路由模型，升级为“连接层定期探活、本地失效主动摘除、Redis 路由租约续约、客户端自动重连恢复”的完整收敛模型，从而在连接异常、实例 crash 和清理失败场景下都能有效防止路由污染。
